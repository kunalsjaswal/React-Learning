// template strings(``) and string interpolation(${})
const name="kunal";
const project="react";

const strTemplate= `hello, my name is ${2>4?name:"null"} and i am learning ${project}.`;

const str=`hello 


its me. string template`;

console.log(strTemplate)
console.log(str)